#include <stdio.h>

int main() {
  /*
   * while (getword() != EOF)
   * if (word is #define) {
   *  next word is the key, following word is value, install them in table
   * }
   * else if (word hashes to key) {
   *  emit value
   * }
   * else emit word to stdout
   */
  while () {
  }
  return 0;
}
